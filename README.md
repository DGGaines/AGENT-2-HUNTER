# AGENT 2.0

Paper multi-asset cheap-launch / pump hunter. One engine. Live-ready ports. LIVE is dead until a single arm.

If it cannot be sold at a $250 clip, it is not a candidate. Empty seats with passers is a MISS.

## Run

```
npm install
npm run desk
```

Desk listens on `127.0.0.1:8787`.

```
npm run test:engine
```

## Oak

See the pull request for the tree: ports (PaperFill / LiveOrder / intel / household / feeds) → one engine → desk canopy. Swap `src/lib/ports/fill.ts` `liveOrder` when you arm live. Do not fork the book.
